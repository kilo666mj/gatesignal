# Copy nginx access records to a dedicated file for shadow-mode evaluation.
# Give access and error streams distinct tags before using `stop` here for a
# final cutover; this example deliberately leaves the original route intact.
if ($programname startswith "nginx_") then {
  action(type="omfile" file="/var/log/gatesignal/access.log")
}
